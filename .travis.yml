# Needed for python 3.7+
dist: xenial

language: python

python:
  - '2.7'
  - '3.5'
  - '3.6'
  - '3.7'
  - '3.8-dev'

install:
  - pip install ansible pycodestyle

script:
  - find . -print
  - python -m pycodestyle .
